{
  "name": "lamproskonstantellos-website",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website, pre-rendered to a static site for Cloudflare Pages.",
  "license": "SEE LICENSE IN LICENSE",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:static": "node build-static.js",
    "test": "node --test",
    "optimize-images": "node scripts/optimize-images.js",
    "build": "npm run optimize-images && node scripts/build.js",
    "watch": "node scripts/build.js --watch"
  },
  "engines": {
    "node": ">=20.9"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright-core": "^1.62.1",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "sharp": "^0.35.3"
  }
}
